# IDEAZ Messenger for Windows

Run `npm install`, then `npm run build`. The installer is created in `dist/`.

Closing the window keeps Messenger active in the Windows system tray so
messages and incoming calls can continue to arrive over the live socket.
Use the tray menu's **Quit** action to exit completely.
